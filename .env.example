# Generate each secret independently, for example with:
# python -c "import secrets; print(secrets.token_urlsafe(48))"
MARKO_PROVISIONING_KEY=replace-with-a-random-secret-at-least-32-characters
MARKO_DEVICE_AUTH_TOKEN=replace-with-another-random-secret-at-least-32-characters
MARKO_PUBLIC_BASE_URL=https://your-device.your-tailnet.ts.net
